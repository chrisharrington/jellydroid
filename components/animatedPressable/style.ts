import { StyleSheet } from 'react-native';

/**
 * Styles for AnimatedPressable component.
 */
const styles = StyleSheet.create({
    container: {
        borderRadius: 8,
        overflow: 'hidden',
    },
});

export default styles;
